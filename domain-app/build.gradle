group 'com.liu'
version '0.0.1-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

dependencies {
   compile project(":domain-base")
    compile('org.liquibase:liquibase-core') //数据库迁移工具
}
